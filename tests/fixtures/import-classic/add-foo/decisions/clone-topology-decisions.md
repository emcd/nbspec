## Decision: Use the fleet's clone topology convention

The project adopts the clone-topology decision format established
by `agents-common add-clone-topology`. Decision notes are stored
under `decisions/<adr>.md` where `<adr>` is the kebab-case
materialization target.

### Status

Accepted 2026-07-12. Drives the import path's filename preservation.