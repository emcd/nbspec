# user-auth (legacy)

## ADDED Requirements

### Requirement: Legacy authentication
The legacy system SHALL authenticate users via the v1 token format.

#### Scenario: Valid v1 token
- **WHEN** a user submits a v1 token
- **THEN** access is granted