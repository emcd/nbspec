# legacy-feature

This legacy archive tree exercises the archive conversion path:
the legacy opsx layout (single `proposal.md` at the archive root,
no `meta.md`, no `work.md`) is tarred as-is under `<id>/tree` with
a synthesized `<id>/meta.json` provenance record.